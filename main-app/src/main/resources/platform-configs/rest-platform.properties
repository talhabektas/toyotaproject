# PF2 Platform Configuration (REST)

# REST API
rest.baseUrl=http://localhost:8080/api/rates
rest.pollingIntervalMs=5000

# Connection Settings
connection.retryCount=3
connection.retryIntervalMs=5000
connection.timeoutMs=10000

# Subscription Settings
subscription.initialRates=PF2_USDTRY,PF2_EURUSD,PF2_GBPUSD