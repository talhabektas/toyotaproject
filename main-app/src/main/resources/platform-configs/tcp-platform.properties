# PF1 Platform Configuration (TCP)

# TCP Connection
tcp.host=localhost
tcp.port=8081

# Connection Settings
connection.retryCount=3
connection.retryIntervalMs=5000
connection.timeoutMs=10000

# Subscription Settings
subscription.initialRates=PF1_USDTRY,PF1_EURUSD,PF1_GBPUSD